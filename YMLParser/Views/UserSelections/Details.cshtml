@using YMLParser.Models
@model YMLParser.Models.Provider

@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Details</h2>

<div>
    <h4>UserSelection</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Link)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Link)
        </dd>

    </dl>
    @Html.ActionLink("Посмотреть файл", "GetFile", new {id = Model.MainOutputFile_Id}, new {@class = "btn btn-primary"})
    <h4>
        @Html.DisplayNameFor(model => model.Categories)
    </h4>
    <div>
        <ul>
        @foreach (Category category in ViewBag.Categories)
        {
            <li class="col-md-4">
                @category.Name;
            </li>
        }
        </ul>
    </div>
</div>
<p>
    @*@Html.ActionLink("Правка", "EditProvider", new { id = Model.Id }) |*@
    @Html.ActionLink("Назад", "Index")
</p>
